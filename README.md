# Svelte + FSD + Vite

Run developmen mode:

```
    npm i
    npm run dev
```

Build and preview: 

```
    npm i
    npm run build
    npm run preview
```

Run tests:

```
    npm run test:unit // Vitest
    npm run test:e2e // Playwright
```
